{
  "name": "entregas-condominio-servidor",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json --routes routes.json --port 9000"
  },
  "author": "Guvidaletti",
  "license": "ISC",
  "devDependencies": {
    "json-server": "^0.17.0"
  }
}
